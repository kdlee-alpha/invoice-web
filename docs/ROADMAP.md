# 견적서 웹 뷰어 (Invoice Web Viewer) 개발 로드맵

노션 데이터베이스 기반 견적서를 웹 브라우저로 조회하고 PDF로 다운로드할 수 있는 최소 기능 제품(MVP)

## 개요

견적서 웹 뷰어는 프리랜서, 1인 사업자, 소규모 에이전시를 위한 견적서 공유 도구로 다음 기능을 제공합니다:

- **노션 API 연동**: 노션 데이터베이스에 입력된 견적 정보를 서버 사이드에서 조회 (60초 캐싱)
- **견적서 웹 뷰**: A4 비율 반응형 레이아웃으로 견적서를 브라우저에 렌더링
- **PDF 다운로드**: Noto Sans KR 한글 폰트 임베딩을 통한 A4 사이즈 PDF 생성 및 다운로드
- **URL 공유**: 클립보드 복사 버튼과 토스트 알림으로 간편한 링크 공유

## 개발 워크플로우

1. **작업 계획**

   - 기존 코드베이스를 학습하고 현재 상태를 파악
   - 새로운 작업을 포함하도록 `docs/ROADMAP.md` 업데이트
   - 우선순위 작업은 마지막 완료된 작업 다음에 삽입

2. **작업 생성**

   - 기존 코드베이스를 학습하고 현재 상태를 파악
   - `/tasks` 디렉토리에 새 작업 파일 생성
   - 명명 형식: `XXX-description.md` (예: `001-setup.md`)
   - 고수준 명세서, 관련 파일, 수락 기준, 구현 단계 포함
   - API/비즈니스 로직 작업 시 "## 테스트 체크리스트" 섹션 필수 포함 (Playwright MCP 테스트 시나리오 작성)
   - 예시를 위해 `/tasks` 디렉토리의 마지막 완료된 작업 참조

3. **작업 구현**

   - 작업 파일의 명세서를 따름
   - 기능과 기능성 구현
   - API 연동 및 비즈니스 로직 구현 시 Playwright MCP로 테스트 수행 필수
   - 각 단계 후 작업 파일 내 단계 진행 상황 업데이트
   - 구현 완료 후 Playwright MCP를 사용한 E2E 테스트 실행
   - 테스트 통과 확인 후 다음 단계로 진행
   - 각 단계 완료 후 중단하고 추가 지시를 기다림

4. **로드맵 업데이트**

   - 로드맵에서 완료된 작업을 체크 표시로 갱신

## 개발 단계

### Phase 1: MVP 핵심 기능 구현 (P0)

- **Task 001: 프로젝트 초기 설정 및 구조 구축** - 완료
  - 프로젝트 라우트 구조 생성 (`/`, `/invoice/[id]`, `/api/invoice/[id]/pdf`)
  - TypeScript 인터페이스 및 타입 정의 (`Invoice`, `InvoiceItem`, `SenderInfo`, `ReceiverInfo`, `InvoiceAmounts`)
  - 공통 레이아웃 컴포넌트 및 shadcn/ui 컴포넌트 설치
  - 환경변수 설정 및 `.env.local.example` 작성

- **Task 002: 노션 API 데이터 조회 (F001)** - 완료
  - 노션 클라이언트 싱글톤 구현 (`src/lib/notion/client.ts`, 지연 초기화 패턴)
  - 노션 API 응답 파싱 유틸 구현 (`src/lib/notion/parser.ts`)
  - `getInvoiceById()` 함수 구현 (`unstable_cache`, 60초 캐싱)
  - 노션 데이터베이스 한국어 필드명 매핑 (프로젝트명, 견적번호, 발행일 등)
  - 오류 처리: 페이지 없음(404), API 오류(500), 접근 권한 없음(403)

- **Task 003: 견적서 웹 뷰 렌더링 (F002)** - 완료
  - `InvoiceView` 메인 렌더링 컴포넌트 구현 (A4 비율 중앙 정렬)
  - `InvoiceHeader` 발신자/수신자 정보, 로고, 견적번호, 날짜 표시
  - `InvoiceTable` 품목 테이블 (번호, 서비스명, 수량, 단가, 금액)
  - `InvoiceSummary` 공급가액, 부가세, 합계 계산 및 표시 (한국 원화 형식)
  - `InvoiceFooter` 메모, 유효기간 표시
  - 반응형 디자인: 데스크탑 A4 카드 중앙 정렬 / 모바일 전체 너비
  - 액션 바: 데스크탑 상단 sticky, 모바일 하단 고정

- **Task 004: PDF 다운로드 기능 (F003)** - 완료
  - `InvoicePdfDocument` @react-pdf/renderer 기반 PDF 문서 컴포넌트 구현
  - PDF Route Handler (`/api/invoice/[id]/pdf`) 서버 사이드 PDF 스트림 응답
  - Noto Sans KR 한글 폰트 임베딩 (Font.register)
  - 파일명 형식: `견적서_[프로젝트명]_[발행일].pdf` (UTF-8 인코딩)
  - A4 사이즈 (210 x 297mm) PDF 생성
  - `PdfDownloadButton` 클라이언트 컴포넌트: 다운로드 상태 관리, 오류 토스트

### Phase 2: MVP 사용성 기능 (P1)

- **Task 005: URL 복사 버튼 (F004)** - 완료
  - `CopyLinkButton` 컴포넌트 구현
  - `navigator.clipboard.writeText()` API 호출
  - 복사 완료 후 sonner 토스트 알림 ("링크가 복사되었습니다")
  - `aria-label="견적서 링크 복사"` 접근성 속성 적용

- **Task 006: 오류 페이지 (F005)** - 완료
  - `not-found.tsx` 404 페이지 구현 (견적서를 찾을 수 없습니다)
  - `error.tsx` 500 오류 페이지 구현 (일시적인 오류 + 다시 시도 버튼)
  - `loading.tsx` 스켈레톤 UI 구현 (A4 카드 비율 스켈레톤)
  - 루트 페이지 (`/`) 안내 메시지 구현

### Phase 3: 배포 및 검증

- **Task 007: 빌드 검증 및 코드 품질 확인** - 우선순위
  - `npm run check-all` 모든 검사 통과 (TypeScript + ESLint + Prettier)
  - `npm run build` 프로덕션 빌드 성공 확인
  - `NOTION_API_KEY`가 클라이언트 번들에 포함되지 않음 확인
  - Playwright MCP를 사용한 E2E 테스트 수행
    - 견적서 페이지 정상 렌더링 확인
    - PDF 다운로드 버튼 동작 확인
    - 링크 복사 버튼 동작 확인
    - 오류 페이지 (404, 500) 표시 확인
    - 모바일 뷰포트(375px) 가독성 확인

- **Task 008: Vercel 배포 및 운영 검증**
  - Vercel 프로젝트 연결 및 환경변수 설정
  - 프로덕션 배포 및 배포 URL 정상 작동 확인
  - 실제 노션 데이터로 견적서 조회 테스트
  - PDF 다운로드 및 한글 폰트 렌더링 최종 확인
  - 모바일/데스크탑 크로스 브라우저 테스트
  - `next.config.ts` 노션 이미지 도메인 허용 설정 확인

- **Task 008-1: MVP 완료 기준 통합 테스트**
  - Playwright MCP를 사용한 전체 사용자 플로우 E2E 테스트
  - F001: 노션 페이지 ID로 견적 데이터 정상 조회
  - F001: 유효하지 않은 ID 입력 시 404 페이지 표시
  - F001: 노션 API 오류 시 500 오류 페이지 표시
  - F001: 데이터 60초 캐싱 동작 확인
  - F002: A4 비율 정상 렌더링, 로고 표시, 금액 계산, 원화 형식
  - F002: 모바일(375px) 가독성 확보
  - F003: PDF 다운로드, 한글 출력, 파일명 형식, A4 사이즈
  - F004: URL 클립보드 복사 및 토스트 알림
  - 에러 핸들링 및 엣지 케이스 테스트 (빈 품목, 긴 프로젝트명 등)

### Phase 4: 향후 계획

#### v2 -- 발신자 관리 기능

- **Task 009: 발신자 인증 시스템 구축**
  - NextAuth.js + Supabase 기반 로그인/인증
  - 세션 관리 및 보안 미들웨어
  - Playwright MCP로 인증 플로우 E2E 테스트

- **Task 010: 견적서 목록 대시보드**
  - 내 견적서 목록 조회 페이지
  - 견적서 상태 관리 (draft -> sent -> accepted)
  - 클라이언트 온라인 승인/반려 버튼

#### v3 -- 자동화 및 고도화

- **Task 011: 이메일 발송 자동화**
  - Resend 연동 이메일 발송
  - 견적 템플릿 저장 및 재사용

- **Task 012: 통계 및 관리 기능**
  - 견적서 열람 통계 (조회 횟수, 최근 열람 시간)
  - 클라이언트 관리 (수신자 주소록)
  - 다국어 지원 (영문 견적서)

#### v4 -- 수익화

- **Task 013: 결제 및 플랜 시스템**
  - 무료/유료 플랜 구분
  - Stripe 월 구독 결제 연동
  - 커스텀 도메인 연결
  - 화이트라벨 지원 (로고/브랜딩 커스터마이징)
